<p>
  <strong>Handle:</strong>
  <%= @tweet.handle %>
</p>

<p>
  <strong>Content:</strong>
  <%= @tweet.content %>
</p>

<%= @tweet.created_at.localtime.strftime("%I:%M%p %b %d %Y") %><br>

<%= link_to 'Edit', edit_tweet_path(@tweet) %> 
<%= link_to "Delete this tweet!", tweet_path, method: :delete, data: { confirm: "Are you sure you want to delete this tweet?"} %>
<%= link_to 'Back', tweets_path %>